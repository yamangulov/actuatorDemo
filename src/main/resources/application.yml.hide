management:
  endpoints:
    web:
      exposure:
        include: health,prometheus #открыть endpoints health и prometheus
  metrics:
    export:
      prometheus:
        enabled: true #включить экспорт Prometheus
    distribution:
      percentiles-histogram:
        "[http.server.requests]": true
